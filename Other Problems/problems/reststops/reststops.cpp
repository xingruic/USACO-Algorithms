#include <bits/stdc++.h>
using namespace std;
int l, n, f, b;
int x[100001], c[100001];
int main() {
    cin >> l >> n >> f >> b;
    for (int i = 1; i <= n; i++) {
        cin >> x[i] >> c[i];
    }
    for (int i = 1; i <= n; i++) {
        // for every rest stop, stop there and eat grass until farmer john catches up
    }
}
