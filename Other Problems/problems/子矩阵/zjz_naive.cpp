#include<iostream>
#include<cstring>
#include<vector>
#include<queue>
#include<map>
#include<algorithm>
using namespace std;
int n, m, r, c, a[16][16];
void dfs(vector<int> rows, vector<int> cols, int ri, int ci) {
    
}
int main() {
    cin >> n >> m >> r >> c;
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {
            cin >> a[i][j];
        }
    }

}